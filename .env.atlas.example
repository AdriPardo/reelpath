DOMAIN=reelpath.atlasops.dev
POSTGRES_PASSWORD=autotube
AUTH_SECRET=reelpath-change-me-auth-secret-min-32-chars
AUTH_REQUIRED=true
NEXT_PUBLIC_AUTH_REQUIRED=true
MOCK_EXTERNAL_APIS=true
ATLAS_HOSTED=true
# Obligatorio: openssl rand -hex 32. Prefer Atlas project secret + envFrom (no inline comment after =).
CREDENTIALS_ENCRYPTION_KEY=
WORKER_CONCURRENCY=1
# METRICS_BEARER_TOKEN=
