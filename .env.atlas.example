# Atlas = producción. Solo secretos/operación; el resto lo fija docker-compose.atlas.yml.
DOMAIN=reelpath.atlasops.dev
POSTGRES_PASSWORD=
AUTH_SECRET=
CREDENTIALS_ENCRYPTION_KEY=
PLATFORM_ADMIN_EMAILS=
WORKER_CONCURRENCY=1

# Claves LLM / ElevenLabs / YouTube OAuth app → Ajustes → Secretos de plataforma (no aquí).
