# Atlas = producción. Solo operación; flags (mocks/auth) los fija docker-compose.atlas.yml.
DOMAIN=reelpath.atlasops.dev
POSTGRES_PASSWORD=autotube
PLATFORM_ADMIN_EMAILS=adripardo72@gmail.com
WORKER_CONCURRENCY=1

# First-boot admin (API runs SEED_DEMO=false seed on start; does not reset existing password)
DEFAULT_ADMIN_EMAIL=adripardo72@gmail.com
DEFAULT_ADMIN_PASSWORD=changeme

# AUTH_SECRET y CREDENTIALS_ENCRYPTION_KEY → Atlas Project → Secrets (nombres exactos) + atlas.yml envFrom.
# LLM / TTS / YouTube OAuth app → Ajustes → Secretos de plataforma (BD Reelpath), no Atlas / no .env.
# git.token / cloudflare.api.token → Atlas Org secrets (no van en este archivo).
